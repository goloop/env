# Variable expansion applies to double-quoted and unquoted values only.
# Single quotes and backticks are literal: `$` stays as-is.
export BASE=world
DOUBLE="hello $BASE"
SINGLE='hello $BASE'
BACKTICK=`hello $BASE`
UNQUOTED=hello-$BASE
