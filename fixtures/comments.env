# The env-file contains random data. This data is intended to check the
# parsing of the file with the comments and empty rows.
export KEY_0="value 0"

# Some comment for the KEY_1
export KEY_1='value 1' # inline comment
export KEY_2=value_2 # inline comment in the plain row

  KEY_3=value_3 # indent before key and comment at the end of the line

KEY_4=value_4:value_4:value_4 # list
KEY_5="some text with # sharp sign and \"escaped quotation\" mark" # comment
